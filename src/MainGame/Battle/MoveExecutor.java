package MainGame.Battle;

import MainGame.Animation.Events.AbilityExecutionEvent;
import javafx.scene.canvas.GraphicsContext;

public final class MoveExecutor
{
    //Constructor
    private MoveExecutor() {}

    //Public Methods
    private static void ExcuteMove(AbilityExecutionEvent e, GraphicsContext gc)
    {
        // Check for halt conditions (e.g. touch attack on fire enemy)

        // Calculate the distance that the attacker needs to move to.

        // Using an animation script, move the attacker that distance
        // within the time amount for the attack.

        // Allowing for damage modifiers, inflict the damage from the incoming attack.

        // Render the damage number animations.
    }
}
